import getDummy from "./dummy.mock";
import getFilters from "./fields.mock";
import { 
    getSingle,
    getThreshold,
} from "./rules.mock";

export default {
    getDummy,
    getFilters,
    getSingle,
    getThreshold,
}
